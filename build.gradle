plugins {
    id("com.android.application") version "8.2.0"
    kotlin("android") version "1.9.20"
}

android {
    namespace = "com.example.tubeme"
    compileSdk = 34
}

dependencies {
    // Empty for root build.gradle
}
